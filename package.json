{
  "name": "yellfy-use",
  "version": "1.0.0",
  "description": "User-friendly require function for Yellfy project",
  "license": "MIT",
  "repository": "mrmlnc/yellfy-use",
  "author": {
    "name": "Denis Malinochkin",
    "url": "canonium.com"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "pretest": "rimraf out && tslint src/**/* && tsc",
    "test": "mocha out/**/*.spec.js",
    "prepublish": "npm run test",
    "start": "rimraf out && tsc --watch"
  },
  "main": "out/use.js",
  "typings": "out/use.d.ts",
  "files": [
    "out/use.js",
    "out/use.d.ts"
  ],
  "keywords": [
    "yellfy",
    "require",
    "use"
  ],
  "dependencies": {
    "camelcase": "^3.0.0"
  },
  "devDependencies": {
    "rimraf": "^2.5.0",
    "typescript": "^2.0.3",
    "tslint": "^4.0.0",
    "mocha": "^3.0.1",
    "gulp-tap": "^0.1.0"
  }
}
